//! `runtime` — the emitter that annotated code expands into.
//!
//! Provides the thread-local trace buffer, the structured `Value` type, the
//! `SpecEvent` / `ToSpecValue` traits, the mock table, JSONL record mode, and
//! the link-time operation/type registry that contract extraction reads.
//!
//! Scaffold stub — implemented in roadmap PR2 + PR3 (issues #2, #3).
