[package]
name = "extract"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Driftwatch extraction driver: binding resolution, runner codegen, build+run+collect, and contract discovery."

[dependencies]
# The CTSC registry contract model this driver derives runtime metadata into.
contract.workspace = true
# The link-time operation/type metadata (`OpMeta`/`TypeMeta`) derivation reads.
runtime.workspace = true

[dev-dependencies]
# Round-trips the derived document through its on-disk JSON form in tests.
serde_json.workspace = true

[lints]
workspace = true
